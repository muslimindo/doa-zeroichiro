services:
  - type: web
    name: doa-zeroichiro
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    autoDeploy: true
